node_modules/
dist/
release/
.DS_Store
*.log
.vscode/
.idea/
recordings/
binaries/
electron-dist/

# MediaPipe camera-background assets — regenerated by scripts/copy-mediapipe-assets.cjs
# on postinstall (WASM copied from node_modules, model downloaded + hash-checked).
public/mediapipe/

# Secrets — NEVER commit. Holds GH_TOKEN used by `pnpm release` to publish to
# GitHub Releases. A leaked token in a public repo is scraped within seconds.
# .env.example is the safe, committed template (no real values).
.env
.env.local

# Local Claude Code settings (per-user, not shared)
.claude/settings.json
.claude/settings.local.json
